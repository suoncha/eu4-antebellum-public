country_decisions = {
	
	ab_swe_teutonic_order = {
		major = yes
		ai_will_do = {
			factor = 0
		}
		potential = {
			OR = {
				tag = SWE
				was_tag = SWE
				tag = GER
				was_tag = GER
			}
			NOT = {
				exists = TEU
			}
			OR = {
				mission_completed = ab_swe_orders
				mission_completed = ab_germany_orders
			}
			NOT = {
				has_country_flag = swe_released_teutons
			}
		}
		allow = {
			is_free_or_tributary_trigger = yes
			is_at_war = no
		}
		effect = {
			custom_tooltip = SWEDEN_TEUTONS
			add_country_modifier = {
				name = swe_teuton_slot
				duration = -1
				desc = SWE_TEUTON_DESC
			}
			hidden_effect = {
				east_prussia_area = {
					limit = {
						owned_by = ROOT
					}
					add_core = TEU
					remove_core = ROOT
				}
				west_prussia_area = {
					limit = {
						owned_by = ROOT
					}
					add_core = TEU
					remove_core = ROOT
				}
				release = TEU
				create_march = TEU
				TEU = {
					set_capital = 41
					change_government = theocracy
					add_government_reform = monastic_order_reform
					add_manpower = 10
					add_treasury = 100
					add_liberty_desire = -50
					change_religion = ROOT
					change_primary_culture = baltic_german
					capital_scope = {
						change_religion = ROOT
					}
				}
			}
			set_country_flag = swe_released_teutons
		}
	}
	
	ab_swe_livonian_order = {
		major = yes
		ai_will_do = {
			factor = 0
		}
		potential = {
			OR = {
				tag = SWE
				was_tag = SWE
				tag = GER
				was_tag = GER
			}
			NOT = {
				exists = LIV
			}
			OR = {
				mission_completed = ab_swe_orders
				mission_completed = ab_germany_orders
			}
			NOT = {
				has_country_flag = swe_released_livonia
			}
		}
		allow = {
			is_free_or_tributary_trigger = yes
			is_at_war = no
		}
		effect = {
			custom_tooltip = SWEDEN_LIVONIANS
			add_country_modifier = {
				name = swe_livonia_slot
				duration = -1
				desc = SWE_LIVONIA_DESC
			}
			hidden_effect = {
				estonia_ingria_area = {
					limit = {
						owned_by = ROOT
					}
					add_core = LIV
					remove_core = ROOT
				}
				curonia_area = {
					limit = {
						owned_by = ROOT
					}
					add_core = LIV
					remove_core = ROOT
				}
				livonia_area = {
					limit = {
						owned_by = ROOT
					}
					add_core = LIV
					remove_core = ROOT
				}
				release = LIV
				create_march = LIV
				LIV = {
					set_capital = 38
					change_government = theocracy
					add_government_reform = monastic_order_reform
					add_manpower = 10
					add_treasury = 100
					add_liberty_desire = -50
					change_religion = ROOT
					change_primary_culture = baltic_german
					capital_scope = {
						change_religion = ROOT
					}
				}
			}
			set_country_flag = swe_released_livonia
		}
	}
	
}